meta {
  name: 04-AddProducto-SinAuth
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/cart/addToCart/cualquier-cart-id/cualquier-product-id
  body: none
  auth: none
}

tests {
  test("Añadir producto sin auth devuelve 401", function() {
    expect(res.status).to.equal(401);
  });
}
