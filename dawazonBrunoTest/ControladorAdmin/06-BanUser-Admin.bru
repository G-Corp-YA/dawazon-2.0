meta {
  name: 06-BanUser-Admin
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/api/users/{{targetUserId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  test("DELETE (ban) usuario como admin devuelve 204", function() {
    expect(res.status).to.equal(204);
  });
}
