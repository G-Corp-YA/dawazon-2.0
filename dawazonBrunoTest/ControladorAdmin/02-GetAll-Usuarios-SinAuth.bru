meta {
  name: 02-GetAll-Usuarios-SinAuth
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/users
  body: none
  auth: none
}

tests {
  test("GET usuarios sin auth devuelve 401", function() {
    expect(res.status).to.equal(401);
  });
}
